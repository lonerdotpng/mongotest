<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>schema</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: #0a0a0a;
            color: white;
            overflow-x: hidden;
        }

        .einfo {
            font-size: 1.2rem;
            color: #ccc;
            margin-top: 1rem;
            color: #905fcc;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(ellipse at center, #1a1a2e 0%, #0a0a0a 70%);
            position: relative;
            overflow: hidden;
        }

        /* Floating Elements */
        .floating-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        .floating-orb {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(45deg, rgba(102, 126, 234, 0.3), rgba(118, 75, 162, 0.3));
            animation: float 8s ease-in-out infinite;
            filter: blur(1px);
        }

        .orb-1 {
            width: 300px;
            height: 300px;
            top: 10%;
            right: 10%;
            animation-delay: 0s;
        }

        .orb-2 {
            width: 200px;
            height: 200px;
            bottom: 20%;
            left: 15%;
            animation-delay: 3s;
        }

        .orb-3 {
            width: 150px;
            height: 150px;
            top: 60%;
            right: 20%;
            animation-delay: 6s;
        }

        /* Translucent Card */
        .translucent-card {
            position: relative;
            z-index: 2;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 3rem;
            max-width: 500px;
            width: 90%;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            animation: fadeInUp 1s ease-out;
        }

        /* Data Fields Styling */
        .data-fields {
            margin: 1.5rem 0;
            text-align: left;
        }

        .field-item {
            display: flex;
            align-items: center;
            margin: 1rem 0;
            padding: 0.8rem 1rem;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            border-left: 4px solid;
            transition: all 0.3s ease;
        }

        .field-item:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }

        .field-name {
            font-weight: 700;
            font-size: 1.1rem;
            margin-right: 0.5rem;
        }

        .field-type {
            font-family: 'Courier New', monospace;
            font-size: 0.95rem;
            padding: 0.2rem 0.6rem;
            border-radius: 6px;
            font-weight: 600;
        }

        /* Individual field colors */
        .field-name-color {
            color: #ff6b6b;
            border-left-color: #ff6b6b;
        }

        .field-name-color .field-type {
            background: rgba(255, 107, 107, 0.2);
            color: #ff9999;
        }

        .field-salary-color {
            color: #4ecdc4;
            border-left-color: #4ecdc4;
        }

        .field-salary-color .field-type {
            background: rgba(78, 205, 196, 0.2);
            color: #7fded8;
        }

        .field-language-color {
            color: #45b7d1;
            border-left-color: #45b7d1;
        }

        .field-language-color .field-type {
            background: rgba(69, 183, 209, 0.2);
            color: #7ac5e0;
        }

        .field-city-color {
            color: #f9ca24;
            border-left-color: #f9ca24;
        }

        .field-city-color .field-type {
            background: rgba(249, 202, 36, 0.2);
            color: #fdd663;
        }

        .field-manager-color {
            color: #a55eea;
            border-left-color: #a55eea;
        }

        .field-manager-color .field-type {
            background: rgba(165, 94, 234, 0.2);
            color: #c485f0;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 1rem 2.5rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
            cursor: pointer;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.6);
            color: WHITE;
            cursor: pointer;
        }

        p {
            font-size: 1.2rem;
            color: #ccc;
            margin-top: 1rem;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px) rotate(0deg);
            }

            50% {
                transform: translateY(-30px) rotate(180deg);
            }
        }
    </style>
</head>

<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="floating-elements">
            <div class="floating-orb orb-1"></div>
            <div class="floating-orb orb-2"></div>
            <div class="floating-orb orb-3"></div>
        </div>

        <div class="translucent-card">
            <div class="data-fields">
                <div class="field-item field-name-color">
                    <span class="field-name">1> name:</span>
                    <span class="field-type">{type: String}</span>
                </div>
                
                <div class="field-item field-salary-color">
                    <span class="field-name">2> salary:</span>
                    <span class="field-type">{type: Number}</span>
                </div>
                
                <div class="field-item field-language-color">
                    <span class="field-name">3> language:</span>
                    <span class="field-type">{type: String}</span>
                </div>
                
                <div class="field-item field-city-color">
                    <span class="field-name">4> city:</span>
                    <span class="field-type">{type: String}</span>
                </div>
                
                <div class="field-item field-manager-color">
                    <span class="field-name">5> ismanager:</span>
                    <span class="field-type">{type: Boolean}</span>
                </div>
            </div>

            <div class="einfo">**Ten records will be created at a time with random combinations of these fields, and if repeated, the earlier record will be deleted.**
            </div><br>
            <a href="/home"><button class="btn-primary">go to Homepage</button></a>

        </div>
    </section>
</body>

</html>